export interface ICTA {
    cta_name: string;
    cta_url: string;
}
export interface Ititle {
    title: string;
    description?: string;
}
export interface IImage {
    image: string;
    image_alt: string;
}